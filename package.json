{
  "name": "agentlings",
  "private": true,
  "version": "0.0.1",
  "license": "Apache-2.0",
  "type": "module",
  "workspaces": [
    "packages/shared",
    "server",
    "web"
  ],
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev -w server\" \"npm run dev -w web\"",
    "serve": "concurrently -n server,web -c blue,green \"npm run serve -w server\" \"npm run dev -w web\"",
    "test": "npm run test -w server && npm run test -w web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build -w web",
    "art": "node scripts/bake-sprites.mjs",
    "art:check": "tsx scripts/check-pack.ts",
    "pack:check": "tsx scripts/check-pack.ts",
    "pack:render": "tsx scripts/render-pack.ts",
    "pack:quantize": "tsx scripts/quantize-pack.ts",
    "ledger:report": "tsx scripts/ledger-report.ts",
    "voice:install": "tsx scripts/voice-install.mts",
    "bench:intake": "tsx scripts/intake-bench.ts",
    "bench:coverage": "tsx scripts/coverage-bench.ts",
    "demo:record": "tsx scripts/record-demo.ts"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "railway": "^3.11.0"
  },
  "dependencies": {
    "docx": "^9.7.1",
    "exceljs": "^4.4.0",
    "jszip": "^3.10.1",
    "mammoth": "^1.12.0",
    "pdf-lib": "^1.17.1",
    "pdf-parse": "^2.4.5",
    "pptxgenjs": "^4.0.1"
  }
}
